; Qt Quick Controls 运行时样式配置。
; Windows 默认加载原生 Windows 风格, 不支持 BikeButton/Field 对
; contentItem/background 的自定义(会产生 "does not support customization"
; 警告且控件外观脱离设计稿)。使用 Basic 风格使自定义生效。
; 该文件需与 bike_client.exe 同目录(或由 windeployqt/打包流程携带)。

[Controls]
Style=Basic
